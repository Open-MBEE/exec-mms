version: 2.1
#orbs:
  #node: circleci/node@1.1.6
  #newman: postman/newman@0.0.2
jobs:
    build: 
        machine: true
        working_directory: ~/mms
        steps:
            - checkout
           # - setup_remote_docker
            - run: 
                name: Install Docker Compose
                command: |
                    set -x
                    sudo curl -L "https://github.com/docker/compose/releases/download/1.26.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
                    sudo chmod +x /usr/local/bin/docker-compose
            #- run: setup_remote_docker
            - run: cp example/src/main/resources/application.properties.example ./example/src/main/resources/application.properties
            - run: node -v
            #- run: docker-compose up --build -d
            #- run: sleep 3m
            #- run: sudo curl -sL https://deb.nodesource.com/setup_11.x | sudo -E bash
            #- run: sudo apt-get install nodejs; node -v
            #- run: sudo npm install -g newman
            - run: curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.35.3/install.sh | bash
            #- run: export NVM_DIR="/opt/circleci/.nvm" || [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
            #[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"
            - run: export NVM_DIR="$([ -z "${XDG_CONFIG_HOME-}" ] && printf %s "${HOME}/.nvm" || printf %s "${XDG_CONFIG_HOME}/nvm")" [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" # This loads nvm 
            #export NVM_DIR="$([ -z "${XDG_CONFIG_HOME-}" ] && printf %s "${HOME}/.nvm" || printf %s "${XDG_CONFIG_HOME}/nvm")"
#[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" # This loads nvm
            - run: nvm install v12.18.1
            - run: node -v
            #- run: cd example; newman run crud.postman_collection.json -e test-env.json --delay-request 300
            #- run: docker inspect mms_mms_1
            #- run: docker inspect mms_postgres_1
            #- run: docker-compose ps
    #test:
        #executor:
            #name: newman/postman-newman-docker
        #steps:
          #- checkout
          #- run: sleep 3m
          #- run: cd example
          #- newman/newman-run: 
              #collection: example/crud.postman_collection.json
              #environment: example/test-env.json
              #delay-request: '300'
workflows:
    version: 2
    build-and-test:
      jobs:
        - build
        #- test
            #requires:
               # - build 